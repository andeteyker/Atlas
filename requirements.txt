# LLM & API
openai>=1.30.0

# Config & Env
python-dotenv>=1.0.0
pyyaml>=6.0

# CLI & UI
rich>=13.7.0
typer>=0.12.0

# Data Validation
pydantic>=2.0.0

# Vector Memory (ChromaDB — lokal, kein Server nötig)
# all-MiniLM-L6-v2 Embedding wird beim ersten Start geladen (~90MB)
chromadb>=0.5.0

# Web Search (kein API-Key nötig)
duckduckgo-search>=6.0.0

# File Locking (Blackboard Concurrency)
filelock>=3.13.0

# File Watching (atlas watch mode)
watchdog>=4.0.0
